#include <iostream>
#include <string>
#include <vector>
#include <map>
using namespace std;
int main(){
    string s ; cin >> s ;
    map<char , int> mp;
    for ( int i = 0 ; i < s.length() ; i++){
        mp[s[i]]++;
    }
    for (pair<char,int> it : mp){
        if (it.second > 1){
            for (int i = 0 ; i < s.length() ; i++){
                if
            }
        }
    }
    return 0;
}